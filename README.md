# Discount Calculator CLI

This program calculates discount based on your number and discount percent input that works in your console.

## How to Run

1. Clone this repo
2. Run "npm ci" to install dependencies
3. Run "npm link" to compile
4. ...then "dccli" to run program
